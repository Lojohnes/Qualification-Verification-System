spring:
  application:
    name: verification-service
  profiles:
    active: dev
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

server:
  port: 8083

aqvp:
  services:
    qualification:
      base-url: ${QUALIFICATION_SERVICE_BASE_URL:http://localhost:8082}
      token: ${QUALIFICATION_SERVICE_TOKEN:}
