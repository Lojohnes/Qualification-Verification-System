# Contributing to AQVP

## Workflow

1. Create a feature branch from `develop`.
2. Apply package-by-module structure and DDD layering.
3. Keep business logic out of controllers and infrastructure.
4. Run `mvn -B -P static-analysis verify` before pushing.
5. Open a pull request and request at least one review.
